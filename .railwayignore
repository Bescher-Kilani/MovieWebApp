# =============================================================================
# RAILWAY DEPLOYMENT - FILES TO IGNORE
# =============================================================================
# This file tells Railway which files/folders NOT to upload during deployment.
# Railway will build dependencies from scratch, so we exclude build artifacts.
# =============================================================================

# =============================================================================
# NODE / NPM
# =============================================================================
# node_modules/ contains all npm packages (~300MB)
# Railway runs "npm install" itself, so uploading these is wasteful
node_modules/

# npm error logs (created when npm install fails)
npm-debug.log*

# yarn error logs (if using yarn instead of npm)
yarn-debug.log*
yarn-error.log*

# pnpm logs (if using pnpm)
pnpm-debug.log*

# =============================================================================
# JAVA / MAVEN
# =============================================================================
# target/ contains compiled .class files and the final .jar (~50MB)
# Railway runs "mvnw clean package" to build a fresh JAR with correct settings
target/

# Compiled Java class files (generated during build)
*.class

# JAR files (final Spring Boot application)
*.jar

# WAR files (we use JAR for Spring Boot, but exclude anyway)
*.war

# Maven wrapper JAR (Railway has Maven pre-installed)
.mvn/wrapper/maven-wrapper.jar

# =============================================================================
# IDE / EDITOR FILES
# =============================================================================
# IntelliJ IDEA project files (personal settings, run configurations)
.idea/
*.iws
*.iml
*.ipr

# VS Code settings (editor config, extensions)
.vscode/

# Vim temporary files (created while editing)
*.swp
*.swo
*~

# Eclipse project files
.project
.classpath
.settings/

# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================
# .env files contain secrets (API keys, database passwords)
# NEVER upload these! Use Railway's Environment Variables instead
.env
.env.local
.env.*.local
.env.production
.env.development

# Keep example file for documentation
!.env.example

# =============================================================================
# VERSION CONTROL
# =============================================================================
# .git/ contains entire repository history (~100MB)
# Railway only needs current code (HEAD commit), not history
.git/

# .gitignore is for Git, Railway uses .railwayignore
.gitignore

# Git attributes (line ending settings)
.gitattributes

# =============================================================================
# LOGS
# =============================================================================
# Log files from local development (can be 50-100MB)
# Railway generates its own logs in the dashboard
*.log
logs/
npm-debug.log*
yarn-debug.log*

# =============================================================================
# TESTING / COVERAGE
# =============================================================================
# Test coverage reports (generated by Jest, Vitest, etc.)
coverage/
.nyc_output/
test-results/

# =============================================================================
# BUILD ARTIFACTS
# =============================================================================
# Frontend build output (created by "npm run build")
# Railway builds this fresh with correct environment variables
Frontend/dist/
Frontend/build/
dist/
build/

# Backend build output (created by "mvnw package")
Backend/target/

# Temporary build files
*.tmp
.cache/

# =============================================================================
# OPERATING SYSTEM
# =============================================================================
# macOS folder metadata (stores icon positions, view settings)
.DS_Store

# Windows thumbnail cache
Thumbs.db

# Windows folder config
desktop.ini

# Linux backup files
*~

# =============================================================================
# DOCUMENTATION (Optional - Remove if needed in Railway)
# =============================================================================
# Markdown documentation files (not needed for app runtime)
*.md

# Keep README for Railway dashboard (shows project info)
!README.md

# Documentation folders
docs/
*.doc
*.docx

# =============================================================================
# LOCAL DEVELOPMENT ONLY
# =============================================================================
# docker-compose.yml is for local multi-container setup
# Railway manages services separately (no docker-compose support)
docker-compose.yml
docker-compose.*.yml

# Local server setup script (for Oracle Cloud, not Railway)
setup-server.sh

# Development Dockerfiles (Railway uses production Dockerfiles)
*.dev.Dockerfile
Dockerfile.dev

# =============================================================================
# MISC
# =============================================================================
# Backup files
*.bak
*.backup
*.old

# Archive files (zips, tars)
*.zip
*.tar
*.gz

# Sensitive files
*.pem
*.key
*.cert

# Database files (if using local SQLite)
*.db
*.sqlite
*.sqlite3

# =============================================================================
# END OF .railwayignore
# =============================================================================